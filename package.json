{
  "name": "@luncheon/esbuild-plugin-jsx-html-entry",
  "version": "0.0.0",
  "description": "An experimental esbuild plugin that generates HTML entry points from JSX.",
  "license": "WTFPL",
  "repository": "luncheon/esbuild-plugin-jsx-html-entry",
  "main": "index.js",
  "keywords": [
    "esbuild",
    "esbuild-plugin",
    "preact"
  ],
  "scripts": {
    "test": "cd test && node build.js"
  },
  "dependencies": {
    "esbuild": ">=0.11.15",
    "preact": "^10.5.13",
    "preact-render-to-string": "^5.1.19"
  },
  "devDependencies": {
    "@luncheon/esbuild-plugin-gzip": "^0.1.0",
    "@types/node": "^15.0.2"
  }
}
